const crudBtn = document.getElementById('crud-btn');
const startElectionBtn = document.getElementById('start-election-btn');

crudBtn.addEventListener('click', () => {
	// navigate to CRUD menu screen
});

startElectionBtn.addEventListener('click', () => {
	// navigate to start election screen
});
